# TCWD GeoPortal - Ultra Minimal Requirements (No Compilation)
# Works with any Python version - zero compilation dependencies

# Core Flask Framework
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# Production WSGI Server
gunicorn==21.2.0

# Basic Security
bcrypt==4.0.1

# HTTP Client
requests==2.31.0

# Environment Variables
python-dotenv==1.0.0

# Note: Removed pandas completely - using native Python + SQLite
# All data processing done with built-in libraries
# CSV module is built-in to Python
